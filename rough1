I am building a real-time voice-based sales training simulator using Azure VoiceLive.

Current Setup:

* Backend is written in Python using Azure VoiceLive SDK
* It uses PyAudio for microphone input and speaker output
* The AI behaves like a "customer" and interacts with a salesperson
* It captures conversation history and generates evaluation at the end
* The system is event-driven and streams audio using WebSockets

Now I want to UPGRADE this system to include an AI Avatar (talking face) in a React frontend.

IMPORTANT:
The current Python backend handles audio input/output using PyAudio, but this WILL NOT WORK for a browser-based avatar system. The browser must handle mic, audio playback, and video rendering.

Your Task:
Refactor the architecture and implement a working solution where:

1. React frontend:

   * Captures microphone input from the browser
   * Streams audio to Azure VoiceLive (or via backend if needed)
   * Receives audio/video stream from Azure Avatar service
   * Displays a real-time talking avatar using a <video> element
   * Handles interruptions (barge-in)
   * Shows conversation transcript in UI

2. Azure Avatar Integration:

   * Use Azure AI Avatar / VoiceLive compatible approach
   * Avatar should lip-sync with generated speech
   * Use WebRTC or streaming API (NOT PyAudio)
   * Maintain low latency

3. Backend (Python - optional role):

   * Remove PyAudio completely
   * Keep only:

     * conversation history tracking
     * evaluation generation logic
   * Expose API endpoint:
     POST /evaluate
     which takes conversation transcript and returns evaluation

4. AI Behavior:

   * Preserve current system prompt:
     "You are a skeptical customer..."
   * Ensure conversation feels realistic:

     * ask questions
     * raise objections
     * discuss pricing, ROI, competitors

5. Event Handling:

   * Handle:

     * speech start
     * speech stop
     * AI response streaming
   * Support interruption (user speaking over AI)

6. Code Requirements:

   * Provide FULL working React code (not pseudo code)
   * Use modern React (hooks)
   * Include setup instructions
   * Show how to connect to Azure VoiceLive / Avatar
   * Show how to stream audio from mic
   * Show how to render avatar video stream

7. Constraints:

   * Do NOT use PyAudio
   * Do NOT rely on local audio playback in Python
   * Everything real-time must happen in browser
   * Keep latency as low as possible

8. Bonus (if possible):

   * Show how to store conversation in frontend and send to backend for evaluation
   * Add a button "End Session" → triggers evaluation

Also provide setup steps at last

Goal:
End result should be a working system where:
User speaks → AI responds → Avatar speaks with lip-sync → conversation is tracked → evaluation is generated.

Do NOT give high-level explanation only. I want IMPLEMENTATION.
