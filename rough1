I have an existing project with the following setup:

Frontend:

* React app (created using Create React App or Vite)
* Runs on http://localhost:3000
* Uses microphone (WebRTC / MediaDevices API) for voice input
* Contains a voice agent UI (avatar + audio streaming)

Backend:

* FastAPI server running separately on http://localhost:8000
* Handles AI + voice processing

Goal:
I want to convert this into a desktop application using Electron, WITHOUT breaking my existing functionality.

IMPORTANT REQUIREMENTS:

* Do NOT modify my existing React components or business logic
* Only add Electron setup around it
* Keep FastAPI running separately (do NOT bundle backend yet)
* Ensure microphone access works inside Electron
* The app must open a desktop window that loads my React app

Your tasks:

1. Project Setup

* Explain where to install Electron (root or frontend folder)
* Install required dependencies (electron, concurrently, etc.)

2. File Creation

* Create a complete `main.js` file for Electron
* Configure BrowserWindow properly
* Load React app via http://localhost:3000
* Enable microphone permissions

3. package.json Changes

* Add "main": "main.js"
* Add scripts:

  * "electron"
  * "desktop" (to run React + Electron together)
* Use concurrently to run both

4. Microphone + Permissions

* Ensure Electron allows microphone access
* Add any required flags or settings

5. Running Instructions

* Exact commands to start:

  * React
  * Electron
  * Both together

6. Folder Structure

* Show final project structure after changes

7. Common Issues + Fixes

* Mic not working
* React not loading
* Port issues

8. Keep it SIMPLE and WORKING

* Do not over-engineer
* Do not introduce TypeScript if not needed
* Do not convert to production build yet

Output format:

* Step-by-step instructions
* Full code blocks (main.js, package.json)
* Clear commands to run

Assume I am a developer but new to Electron, so clarity is important.
