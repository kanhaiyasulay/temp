I already have a working Electron + React + FastAPI desktop app setup.

Current setup:

* Electron launches React frontend (http://localhost:3000)
* Electron auto-starts FastAPI backend using Python (uvicorn main:app --port 8000)
* Everything works correctly

Goal:
I want to remove the Python dependency by converting my FastAPI backend into a standalone executable (.exe) using PyInstaller, and then update Electron to run that .exe instead of Python.

IMPORTANT REQUIREMENTS:

* Do NOT break my existing working setup
* Keep Electron + React behavior unchanged
* Only modify backend packaging and Electron backend startup logic
* Ensure .env file is still loaded correctly
* Ensure backend still runs on port 8000
* Keep implementation simple and reliable

Your tasks:

1. Create a proper entry script for PyInstaller

* Create a new file (e.g., run_backend.py)
* It should start FastAPI using uvicorn programmatically
* Equivalent to: uvicorn main:app --port 8000

2. Add PyInstaller setup

* Install PyInstaller
* Provide the exact command to generate a single executable
* Use --onefile

3. Handle environment variables

* Ensure .env file is loaded correctly in the executable
* If needed, use python-dotenv or similar approach
* Ensure .env works when placed next to the .exe

4. Build the executable

* Show expected folder structure after build
* Output should be something like dist/run_backend.exe

5. Test instructions

* How to run the .exe manually
* How to verify FastAPI is working (localhost:8000)

6. Update Electron main process

* Replace Python spawn with .exe spawn
* Use correct path resolution (cross-platform safe if possible)
* Ensure working directory is set so .env loads properly

7. Add robustness

* Proper logging from backend process
* Handle failure if .exe not found
* Ensure backend process is killed on app exit

8. Do NOT:

* Change frontend or ports
* Introduce unnecessary complexity
* Bundle everything into installer yet (we will do that later)

Output format:

* Step-by-step instructions
* Full code for:

  * run_backend.py
  * updated main.js (only relevant parts)
* Exact commands to run
* Final folder structure

Assume I will directly copy and execute your instructions, so accuracy is critical.
